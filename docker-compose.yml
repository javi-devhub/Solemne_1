version: "3.9"

services:
  api:
    build: .
    container_name: solemne_api
    ports:
      - "8000:8000"
    command: uv run python -m uvicorn main:app --host 0.0.0.0 --port 8000
    volumes:
      - .:/app
  tests:
    build: .
    command: uv run pytest
